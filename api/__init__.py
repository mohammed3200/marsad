"""marsad web API — Qt-free FastAPI layer over the proven Python core.

`services.AppService` mirrors the Qt `AppController` (backend/controller.py)
without any Qt dependency; `app` exposes it as REST + WebSocket.
"""

# مرصد — marsad — LTT PM Intelligence Desktop
# Python 3.10+
# Qt Quick / QML front-end + report exporters. Arabic shaping in the UI is
# handled natively by Qt (HarfBuzz); the PDF exporter needs reshaper+bidi
# (reportlab shapes nothing on its own).

PySide6>=6.8        # Qt Quick / QML UI runtime
reportlab>=4.0      # PDF export
arabic-reshaper>=3.0  # Arabic letter shaping for the PDF exporter
python-bidi>=0.4    # BiDi ordering for the PDF exporter
openpyxl>=3.1       # Excel export + reading ERP .xlsx inputs
PyPDF2>=3.0         # extract text from PDF email/ERP attachments (optional at runtime)
python-docx>=1.1    # read Word .docx field reports (upload + ERP folder)
qrcode>=7.4         # WhatsApp link QR rendered in-app (matrix only, no PIL)
fastapi>=0.110      # Qt-free web API (optional alternate interface)
uvicorn[standard]>=0.27  # ASGI server for the API
